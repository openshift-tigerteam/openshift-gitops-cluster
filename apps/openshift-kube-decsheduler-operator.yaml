apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: openshift-kube-decsheduler-operator
  namespace: openshift-gitops-cluster
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  project: cluster
  source:
    repoURL: https://github.com/openshift-tigerteam/openshift-gitops-cluster.git
    targetRevision: main
    path: openshift-kube-decsheduler-operator
  destination:
    server: https://kubernetes.default.svc
    namespace: openshift-kube-decsheduler-operator
  syncPolicy:
    automated:
      prune: true
      selfHeal: true